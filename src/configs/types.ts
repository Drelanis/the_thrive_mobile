export type CompanyType = {
  id: string;
  name: string;
  email: string;
  password: string;
  currency_id: string;
  createdAt: Date;
  updatedAt: Date;
};

export type DirectionType = {
  direction: string;
  description: string;
};
